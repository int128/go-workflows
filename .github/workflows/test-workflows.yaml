name: test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    uses: ./.github/workflows/check.yaml
    permissions:
      contents: write
      pull-requests: write
    with:
      go-version: 1.21.3
      golangci-lint-version: v1.55.1
